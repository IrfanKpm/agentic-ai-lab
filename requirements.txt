# Environment management
python-dotenv

# Web app UI framework
streamlit

# Backend API server (FastAPI stack)
fastapi
uvicorn
sse-starlette
langserver   # language server support (if used for tooling/autocomplete)

# LangChain core + integrations (LLMs, tools, and community modules)
langchain
langchain-community
langchain-openai
langchain-google-genai
langchain-huggingface

# RAG (Retrieval-Augmented Generation) dependencies
sentence-transformers
langchain-chroma   # Chroma vector database (persistent, LangChain-supported wrapper)
faiss-cpu          # Vector database (fast similarity search, in-memory)
beautifulsoup4     # HTML parsing for web scraping
playwright         # browser automation for dynamic content scraping
pypdf              # reading, extracting, and manipulating PDF files